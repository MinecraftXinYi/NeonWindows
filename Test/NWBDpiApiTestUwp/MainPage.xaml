<Page
    x:Class="NWBDpiApiTestUwp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NWBDpiApiTestUwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="10">
        <StackPanel>
            <StackPanel>
                <TextBlock Text="Common API Call" Style="{StaticResource CaptionTextBlockStyle}"/>
                <TextBlock x:Name="infoblock1" FontSize="15" IsTextSelectionEnabled="True"/>
                <TextBlock x:Name="infoblock2" FontSize="15" IsTextSelectionEnabled="True"/>
                <TextBlock x:Name="infoblock3" FontSize="15" IsTextSelectionEnabled="True"/>
                <TextBlock x:Name="infoblock4" FontSize="15" IsTextSelectionEnabled="True"/>
                <TextBlock x:Name="infoblock5" FontSize="15" IsTextSelectionEnabled="True"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Native API Call" Style="{StaticResource CaptionTextBlockStyle}"/>
                <TextBlock x:Name="infoblock6" FontSize="15" IsTextSelectionEnabled="True"/>
                <TextBlock x:Name="infoblock7" FontSize="15" IsTextSelectionEnabled="True"/>
                <TextBlock x:Name="infoblock8" FontSize="15" IsTextSelectionEnabled="True"/>
                <TextBlock x:Name="infoblock9" FontSize="15" IsTextSelectionEnabled="True"/>
                <TextBlock x:Name="infoblock10" FontSize="15" IsTextSelectionEnabled="True"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
